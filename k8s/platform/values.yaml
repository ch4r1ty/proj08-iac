# The deployment script sets these to node1's floating IP so MLflow and MinIO
# are reachable from a laptop through Chameleon security groups.
minio:
  externalIP: ""

mlflow:
  externalIP: ""

gateway:
  enabled: false
  externalIP: ""
